# Task #21 - extensions_time_budget - Proof Document

## Summary

Implemented `extensions_time_budget` as a root-level Optional argument with Default value `"PT1H30M"` and validation for ISO 8601 duration format between PT15M (15 minutes) and PT2H (2 hours). The field is updatable (not ForceNew) and maps to `properties.extensionsTimeBudget` in the Azure API.

## Shadow Implementation

```hcl
# variables.tf
variable "extensions_time_budget" {
  type        = string
  default     = "PT1H30M"  # <-
  nullable    = false  # <-
  description = "(Optional) Specifies the duration allocated for all extensions to start. The time duration should be between 15 minutes and 120 minutes (inclusive) and should be specified in ISO 8601 format. Defaults to `PT1H30M`."

  validation {  # <-
    condition = can(regex("^PT([0-9]+H)?([0-9]+M)?([0-9]+S)?$", var.extensions_time_budget))  # <-
    error_message = "extensions_time_budget must be a valid ISO 8601 duration (e.g., PT1H30M, PT15M, PT2H)."  # <-
  }  # <-

  validation {  # <-
    condition = (  # <-
      can(regex("^PT([0-9]+H)?([0-9]+M)?([0-9]+S)?$", var.extensions_time_budget)) ?  # <-
      (  # <-
        # Parse hours, minutes, seconds  # <-
        (length(regexall("([0-9]+)H", var.extensions_time_budget)) > 0 ? tonumber(regexall("([0-9]+)H", var.extensions_time_budget)[0][0]) : 0) * 3600 +  # <-
        (length(regexall("([0-9]+)M", var.extensions_time_budget)) > 0 ? tonumber(regexall("([0-9]+)M", var.extensions_time_budget)[0][0]) : 0) * 60 +  # <-
        (length(regexall("([0-9]+)S", var.extensions_time_budget)) > 0 ? tonumber(regexall("([0-9]+)S", var.extensions_time_budget)[0][0]) : 0)  # <-
      ) >= 900 && (  # <-
        (length(regexall("([0-9]+)H", var.extensions_time_budget)) > 0 ? tonumber(regexall("([0-9]+)H", var.extensions_time_budget)[0][0]) : 0) * 3600 +  # <-
        (length(regexall("([0-9]+)M", var.extensions_time_budget)) > 0 ? tonumber(regexall("([0-9]+)M", var.extensions_time_budget)[0][0]) : 0) * 60 +  # <-
        (length(regexall("([0-9]+)S", var.extensions_time_budget)) > 0 ? tonumber(regexall("([0-9]+)S", var.extensions_time_budget)[0][0]) : 0)  # <-
      ) <= 7200 : true  # <-
    )  # <-
    error_message = "extensions_time_budget must be between PT15M (15 minutes = 900 seconds) and PT2H (2 hours = 7200 seconds)."  # <-
  }  # <-
}  # <-

# migrate_main.tf
locals {
  body = {
    properties = merge(
      {
        # ... other fields
        evictionPolicy = var.eviction_policy
        extensionsTimeBudget = var.extensions_time_budget  # <-
      },
      # ... rest of merge
    )
  }
}
```

## Create Phase Verification

**Query Result:** The Create method shows this is a **single-phase** resource with one primary `CreateOrUpdateThenPoll` operation.

**Go Code Evidence (Create method):**
```go
params := virtualmachines.VirtualMachine{
	// ...
	Properties: &virtualmachines.VirtualMachineProperties{
		// ...
		ExtensionsTimeBudget:   pointer.To(d.Get("extensions_time_budget").(string)),
	},
	// ...
}

if err := client.CreateOrUpdateThenPoll(ctx, id, params, virtualmachines.DefaultCreateOrUpdateOperationOptions()); err != nil {
	return fmt.Errorf("creating Linux %s: %+v", id, err)
}
```

**Classification:** This field is set BEFORE the primary create operation in the initial request payload.

**Decision:** Field belongs in `local.body.properties.extensionsTimeBudget` (standard Create phase).

## Assignment Path Verification

**Predicted Path:** `properties.extensionsTimeBudget`

**Go Code Evidence:**
```go
// Step 1: Field assigned to properties
params := virtualmachines.VirtualMachine{
	Properties: &virtualmachines.VirtualMachineProperties{
		ExtensionsTimeBudget: pointer.To(d.Get("extensions_time_budget").(string)),
	},
}

// Step 2: VirtualMachine struct has Properties field
type VirtualMachine struct {
	Properties *VirtualMachineProperties `json:"properties,omitempty"`
}

// Step 3: VirtualMachineProperties struct has ExtensionsTimeBudget field
type VirtualMachineProperties struct {
	ExtensionsTimeBudget *string `json:"extensionsTimeBudget,omitempty"`
}
```

**Verified Path:** `properties.extensionsTimeBudget`

**Path Comparison:** ✅ **MATCH** - Predicted path matches actual assignment path.

## Provider Schema

**Go Source Evidence:**
```go
"extensions_time_budget": {
	Type:         pluginsdk.TypeString,
	Optional:     true,
	Default:      "PT1H30M",
	ValidateFunc: azValidate.ISO8601DurationBetween("PT15M", "PT2H"),
},
```

**Key Properties:**
- **Type:** String
- **Optional:** true
- **Default:** `"PT1H30M"` (1 hour 30 minutes)
- **ForceNew:** false (not present, meaning it's updatable)
- **Validation:** `ISO8601DurationBetween("PT15M", "PT2H")` - between 15 minutes and 2 hours

## Azure API Schema

**Resource Type:** `Microsoft.Compute/virtualMachines@2024-03-01`

**API Schema Evidence:**
```
properties.extensionsTimeBudget: String (optional)
```

**Property Path:** `properties.extensionsTimeBudget`

**Description:** Specifies the time alloted for all extensions to start. The time duration should be between 15 minutes and 120 minutes (inclusive) and should be specified in ISO 8601 format. The default value is 1 hour 30 minutes (PT1H30M).

## Mapping

| Terraform (snake_case) | Azure API (camelCase) |
|------------------------|----------------------|
| extensions_time_budget | extensionsTimeBudget |

## Special Handling

### 1. Default Value

The provider schema specifies `Default: "PT1H30M"`, which means the variable must have:
- `default = "PT1H30M"`
- `nullable = false` (CRITICAL: root-level arguments with defaults MUST have `nullable = false`)

### 2. Validation

The provider uses `azValidate.ISO8601DurationBetween("PT15M", "PT2H")` which validates:
1. **Format validation:** ISO 8601 duration format (e.g., PT1H30M, PT15M, PT2H, PT90M, PT1H)
2. **Range validation:** Between 15 minutes (900 seconds) and 2 hours (7200 seconds)

**Implementation:**
- First validation block: Check format is valid ISO 8601 duration (PT + optional hours/minutes/seconds)
- Second validation block: Parse duration to seconds and check range 900-7200 seconds

### 3. Update Support

**Go Code Evidence (Update method):**
```go
if d.HasChange("extensions_time_budget") {
	shouldUpdate = true
	update.Properties.ExtensionsTimeBudget = pointer.To(d.Get("extensions_time_budget").(string))
}
```

**Decision:** Field is updatable without requiring deallocation or shutdown. Not added to `replace_triggers_external_values`.

### 4. No ConflictsWith

The field has no `ConflictsWith` constraints in the schema, so it can be set alongside any other field including `os_managed_disk_id`.

## Hidden Fields

None. This is a standard root-level argument with no hidden companion fields.

## Deferred Work Completion

Checked `following.md` - no work was deferred to Task #21.

## Critical Review & Edge Case Analysis

### Null Semantics
- **Variable default:** `"PT1H30M"` (explicit default, never null)
- **API behavior:** If not specified, Azure uses PT1H30M as the default
- **Implementation:** Variable has explicit default matching provider behavior

### Boundary Conditions
- **Minimum:** PT15M (15 minutes = 900 seconds) - validated
- **Maximum:** PT2H (2 hours = 7200 seconds) - validated
- **Default:** PT1H30M (1 hour 30 minutes = 5400 seconds) - within range

### Valid ISO 8601 Duration Examples
- `PT15M` - 15 minutes (minimum)
- `PT30M` - 30 minutes
- `PT1H` - 1 hour
- `PT1H30M` - 1 hour 30 minutes (default)
- `PT90M` - 90 minutes (same as PT1H30M)
- `PT2H` - 2 hours (maximum)
- `PT120M` - 120 minutes (same as PT2H)

### Invalid Examples (caught by validation)
- `PT5M` - too short (< 15 minutes)
- `PT3H` - too long (> 2 hours)
- `90M` - missing PT prefix
- `1H30M` - missing PT prefix
- `PT1.5H` - decimal not allowed in ISO 8601 duration

### Idempotency
- Direct value assignment: `extensionsTimeBudget = var.extensions_time_budget`
- No array operations or ordering concerns
- ✅ Idempotent

### Safe References
- No nested object access
- Direct string value
- ✅ Safe

## Checklist

- ✅ Property in correct local (`local.body.properties.extensionsTimeBudget`)
- ✅ ForceNew: N/A (field is updatable, not ForceNew)
- ✅ All logic EXACTLY replicated from provider (Default value, ISO8601 format validation, range validation)
- ✅ Validations IMPLEMENTED in variables.tf (format and range validations added)
- ✅ TODO comment: N/A (no sensitive fields, no independent ephemeral variables)
- ✅ Hidden fields checked (none found)
- ✅ Deferred work in following.md: N/A (no work deferred to other tasks)
- ✅ Deferred work from following.md: Checked - no work deferred to Task #21
- ✅ Critical review (null semantics: explicit default; edge cases: min/max validated; idempotency: yes; safe refs: yes)
- ✅ Edge Case Analysis in proof (included above)
- ✅ Proof created
- ✅ `track.md` update pending
- ✅ Self-Review: Only added extensions_time_budget field, no other task content included

---

## ✅ CHECKER VALIDATION - APPROVED

**Checked by:** Checker Agent
**Date:** 2025-12-24
**Task:** #21 - extensions_time_budget

### Validation Results

✅ **ForceNew Logic:** Not ForceNew - field is updatable without forcing replacement (correctly not added to replace_triggers_external_values)
✅ **Stable Keys:** N/A (no ForceNew triggers needed)
✅ **Phase Detection:** Field correctly placed in `local.body.properties.extensionsTimeBudget` (Create phase)
✅ **Type Conversion:** Direct string assignment - no conversion needed
✅ **Null Handling:** Variable has explicit default "PT1H30M" with `nullable = false` (correct per executor.md for root-level defaults)
✅ **Validations:** All provider validations EXACTLY replicated:
  - Format validation: ISO 8601 duration pattern `^PT([0-9]+H)?([0-9]+M)?([0-9]+S)?$`
  - Range validation: Between 900 seconds (PT15M) and 7200 seconds (PT2H) with exact parsing logic
✅ **Default Value:** Correctly implemented with both `default = "PT1H30M"` AND `nullable = false` (mandatory per executor.md lines 405-430 for root-level arguments with defaults)
✅ **Deferred Work Completion:** No deferred work for this task (checked following.md)
✅ **Deferred Work Recording:** No deferrals made by this task
✅ **Edge Cases:** Comprehensive analysis provided - boundary conditions validated, idempotency confirmed, safe references verified

### Compliance Statement

This implementation EXACTLY replicates the provider behavior as required by `executor.md`. All validations from the provider schema have been precisely replicated in Terraform validation blocks. The default value handling follows the mandatory pattern for root-level arguments with defaults (both `default` and `nullable = false` set). No deviations, simplifications, or "safer alternatives" were found.

**Specific Validation Checks Passed:**
1. ✅ Provider schema default "PT1H30M" exactly matches variable default
2. ✅ `nullable = false` correctly set (executor.md line 405-430 requirement)
3. ✅ ISO8601DurationBetween validation logic precisely replicated with regex parsing
4. ✅ Range boundaries exact: 900-7200 seconds (PT15M to PT2H)
5. ✅ Field placed in correct location: `properties.extensionsTimeBudget` (verified with Go source)
6. ✅ Update support correctly identified (not ForceNew)
7. ✅ No ConflictsWith constraints (verified in schema)
8. ✅ Proof document contains all mandatory sections with proper evidence
9. ✅ No forbidden phrases or justifications for deviations found

**Status:** APPROVED ✅

---
